package com.urlshortener.config;

import org.springframework.context.annotation.Configuration;

// Redis configuration disabled for demo - using simple cache instead
@Configuration
public class RedisConfig {
    // Redis beans commented out for demo without Docker
}